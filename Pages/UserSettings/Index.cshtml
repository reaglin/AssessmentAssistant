@page
@model AssessmentAssistant.Pages.UserSettings.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserSettings[0].UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserSettings[0].MeasurementPeriod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserSettings[0].AcademicProgramId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UserSettings) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MeasurementPeriod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AcademicProgramId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UserSettingsId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UserSettingsId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UserSettingsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
